{
  "name": "simplisign-enterprise",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "mkdir -p dist && tailwindcss -i src/input.css -o dist/output.css --minify && cp *.html dist/ && cp -r images dist/ 2>/dev/null || true"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.39.0"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.3.0",
    "tailwindcss": "^4.3.0"
  }
}
